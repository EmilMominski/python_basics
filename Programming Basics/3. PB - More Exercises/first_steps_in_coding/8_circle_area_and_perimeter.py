# Data Input
import math
radius = float(input())

# Logic
calculated_area = math.pi * radius ** 2
calculated_parameter = 2 * math.pi * radius

# Print Output
print(f"{calculated_area:.2f}")
print(f"{calculated_parameter:.2f}")
